/** Re-enable when the 3D globe UX is ready for production. */
export const MAP_3D_ENABLED = false;

/**
 * Viewport bbox index + map "Explore here" / Here-mode feed sync.
 * Off until geo-aware discovery is product-ready.
 */
export const SPATIAL_VIEWPORT_DISCOVERY_ENABLED = false;
