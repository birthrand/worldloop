/** Bundled `data/countries.json` powers Explore, search, and map without backend feed APIs. */
export const STATIC_COUNTRY_CATALOG_ENABLED = true;

/** Bundled `data/country-profiles.json` powers offline Wikipedia + landmarks on country detail. */
export const STATIC_COUNTRY_PROFILE_CATALOG_ENABLED = true;

/** Culture tab reads `videos[]` from the static catalog instead of `/feed/culture/countries`. */
export const STATIC_CULTURE_VIDEOS_ENABLED = true;
